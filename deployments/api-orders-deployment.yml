apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-orders-deployment
  labels:
    app: api-orders-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-orders-deployment
  template:
    metadata:
      labels:
        app: api-orders-deployment
    spec:
      containers:
        - name: api-orders
          image: ghcr.io/goodfood-cesi/api-orders:latest
      imagePullSecrets:
        - name: ghcr-secret
